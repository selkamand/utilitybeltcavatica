#' Roxygen Cache
#'
#' Place to stash roxygen descriptions for common paramaters used accross package
#'
#' @param project a sevenbridges 'Project' object. See readme for instructions on how to create this object
#' @param app_id  unique app id: <project_id>/<short_app_id>/<version_number>. Can find this by running cavatica_app_list_all, cavatica_app_search, or using web platform (ID is everything after # in URL. May still need to add /<version_number>) (character)
standard_description_function <- function(project, app_id ){

}
